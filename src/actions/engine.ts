"use server";

// Engine Server Actions
// Note: AI synthesis streams via POST /api/engine/analyse (not a Server Action,
// because Server Actions don't support streaming responses).
//
// Suggestion accept / edit / dismiss actions are implemented in Prompt 16.
